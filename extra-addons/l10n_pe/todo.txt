Por hacer:

#Agregar este contraint a nivel de padre, solo sino se utiliza base_vat
class res_partner(osv.osv):
    _name = 'res.partner'
    _sql_constraints = [
        ('ref_unique','unique(ref)','The reference (RUC) must be unique'),
    ]
    

#incluir el contraint en product_template
class product_template(osv.osv):
    _name = 'product.template'

    _sql_constraints = [
        ('default_code_unique','unique(default_code)','The default code must be unique'),
    ]
    
